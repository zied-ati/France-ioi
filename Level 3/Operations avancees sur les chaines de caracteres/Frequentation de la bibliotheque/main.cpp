#include <bits/stdc++.h>

using namespace std;
string s,s1,s2;
int n;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int sum=0;
    while(cin>>n)
      sum+=n;
    cout<<sum<<endl;

    return 0;
}
