#include <bits/stdc++.h>
#define N 128

using namespace std;
string s1,s2,s;
int tab[N]={0};

int main()
{
    ios_base::sync_with_stdio(0);
    int n,m;
    getline(cin,s);
      for(int i=0;i<s.length();++i)
         cout<<s[i]<<endl;

    return 0;
}